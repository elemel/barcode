#!/usr/bin/env python3

from underbar.stdio import StandardStream

for i, handle in enumerate(StandardStream):
    if i:
        print()

    print(f'{handle.name.lower()}:')
    print(f'    {handle.value}')
